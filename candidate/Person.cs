namespace candidate
{
    public class Person
    {
      private string last;
      private string first;
      Address address;
        public Person(string lastName, string firstName, Address addr)
        {
            last = lastName;
            first = firstName;
            address = addr;
        }
 
       
    }
}
